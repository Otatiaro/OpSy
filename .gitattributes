# Files that should not appear in git-archive output (release zips).
# CI scaffolding and dev metadata are not useful when dropping OpSy into
# another project as plain sources.
.github/         export-ignore
tests/           export-ignore
.gitattributes   export-ignore
.gitignore       export-ignore
